<h1>Students</h1>

<ul>
  <% @students.each do |student| %>
    <li><%= link_to student.name, house_student_path(student.house, student) %> - <%= link_to student.house.name, house_path(student.house) %></li>
  <% end %>
</ul>